# FNF Studio — Workflow Architecture

> 4개 핵심 워크플로의 파이프라인, 코어 모듈, 스킬 연결도

---

## Overview: 공통 인프라

```
                                    ┌─────────────────────────────────────────────┐
                                    │            Shared Infrastructure            │
                                    ├─────────────────────────────────────────────┤
                                    │                                             │
                                    │   ┌──────────────┐   ┌──────────────────┐  │
                                    │   │ core/config   │   │ core/options     │  │
                                    │   │  IMAGE_MODEL  │   │  ASPECT_RATIOS   │  │
                                    │   │  VISION_MODEL │   │  RESOLUTIONS     │  │
                                    │   └──────┬───────┘   │  COST_TABLE      │  │
                                    │          │           └────────┬─────────┘  │
                                    │          │                    │            │
                                    │   ┌──────┴────────────────────┴─────────┐  │
                                    │   │           core/api                   │  │
                                    │   │   get_next_api_key() — 키 로테이션  │  │
                                    │   └─────────────────────────────────────┘  │
                                    │                                             │
                                    │   ┌─────────────────────────────────────┐  │
                                    │   │       core/validators/              │  │
                                    │   │  base.py    — WorkflowType enum     │  │
                                    │   │             — WorkflowValidator ABC  │  │
                                    │   │             — CommonValidationResult │  │
                                    │   │  registry.py — @register decorator  │  │
                                    │   └─────────────────────────────────────┘  │
                                    │                                             │
                                    │   ┌─────────────────────────────────────┐  │
                                    │   │       core/outfit_analyzer          │  │
                                    │   │  OutfitAnalyzer — 착장 VLM 분석    │  │
                                    │   │  (브랜드컷, 인플루언서, 착장스왑 공용) │  │
                                    │   └─────────────────────────────────────┘  │
                                    │                                             │
                                    └─────────────────────────────────────────────┘
```

---

## 1. 브랜드컷 (Brand Cut)

**스킬:** `.claude/skills/브랜드컷_brand-cut/SKILL.md`
**코어:** `core/brandcut/`
**컨셉:** 얼굴+착장 이미지 기반 화보 전체 생성 (한국어 JSON 스키마 프롬프트)

```
  ┌─────────┐     ┌─────────┐     ┌─────────┐
  │  얼굴   │     │  착장   │     │ 포즈/표정│
  │ 이미지  │     │ 이미지  │     │ 레퍼런스│
  └────┬────┘     └────┬────┘     └────┬────┘
       │               │               │
       └───────┬───────┘               │
               │                       │
               ▼                       ▼
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │   ┌──────────────────────────────────────────────┐     │
  │   │           1. VLM 분석 (VISION_MODEL)         │     │
  │   │                                              │     │
  │   │  ┌──────────────┐  ┌───────────────────────┐ │     │
  │   │  │ analyzer.py  │  │ (외부 위임)            │ │     │
  │   │  │              │  │                       │ │     │
  │   │  │ analyze_     │  │ ai_influencer/        │ │     │
  │   │  │  outfit() ───┼──▶ pose_analyzer.py     │ │     │
  │   │  │              │  │ expression_analyzer.py│ │     │
  │   │  └──────────────┘  └───────────────────────┘ │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       2. 프롬프트 조립                        │     │
  │   │                                              │     │
  │   │  ┌────────────────────┐  ┌────────────────┐  │     │
  │   │  │prompt_builder_v2.py│  │style_director.py│ │     │
  │   │  │                    │  │                 │ │     │
  │   │  │ build_prompt()     │  │ A급 DB에서      │ │     │
  │   │  │ 한국어 JSON 스키마 │  │ 스타일 레퍼런스 │ │     │
  │   │  │ + 금지조합 자동수정│  │ 자동 선택       │ │     │
  │   │  └────────────────────┘  └────────────────┘  │     │
  │   │                                              │     │
  │   │  ┌────────────────────┐  ┌────────────────┐  │     │
  │   │  │  templates.py      │  │ db/mlb_style/  │  │     │
  │   │  │  POSE_REF_TAG      │  │ A급 이미지 DB  │  │     │
  │   │  │  EXPRESSION_REF_TAG│  │ director JSON  │  │     │
  │   │  └────────────────────┘  └────────────────┘  │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       3. 이미지 생성 (IMAGE_MODEL)            │     │
  │   │                                              │     │
  │   │  ┌───────────────────────────────────────┐   │     │
  │   │  │ generator_v2.py                       │   │     │
  │   │  │                                       │   │     │
  │   │  │ generate_brandcut()                   │   │     │
  │   │  │                                       │   │     │
  │   │  │ API Parts 전송 순서:                  │   │     │
  │   │  │  1. 포즈 레퍼런스 img                 │   │     │
  │   │  │  2. 표정 레퍼런스 img                 │   │     │
  │   │  │  3. 프롬프트 텍스트 (한국어)          │   │     │
  │   │  │  4. 스타일 레퍼런스 img               │   │     │
  │   │  │  5. 카메라/표정 강조 텍스트           │   │     │
  │   │  │  6. 착장 아이템 강조 텍스트           │   │     │
  │   │  │  7. 얼굴 imgs (FACE REFERENCE)        │   │     │
  │   │  │  8. 착장 imgs (OUTFIT REFERENCE)      │   │     │
  │   │  └───────────────────────────────────────┘   │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       4. 검수 + 재생성 루프                   │     │
  │   │                                              │     │
  │   │  ┌─────────────────┐  ┌────────────────────┐ │     │
  │   │  │validator_v2.py  │  │retry_generator_v2  │ │     │
  │   │  │                 │  │                    │ │     │
  │   │  │ 14개 기준 검수  │  │ max_retries=2     │ │     │
  │   │  │ Pass: 총점 85+  │  │ 실패 항목별       │ │     │
  │   │  │                 │  │ 타겟 강화          │ │     │
  │   │  │ face_identity   │  │ Temp 0.03씩 감소  │ │     │
  │   │  │   13% (최고)    │  │                    │ │     │
  │   │  └────────┬────────┘  └──────────┬─────────┘ │     │
  │   │           │   FAIL               │           │     │
  │   │           └──────────────────────▶│           │     │
  │   │                                   │ PASS      │     │
  │   └───────────────────────────────────┼───────────┘     │
  │                                       │                 │
  │                                       ▼                 │
  │                              ┌─────────────────┐        │
  │                              │    결과 반환     │        │
  │                              │ image + score    │        │
  │                              │ + criteria       │        │
  │                              │ + history        │        │
  │                              └─────────────────┘        │
  │                                                        │
  │  ┌────────────────┐   ┌──────────────┐                 │
  │  │clip_validator  │   │edit_generator │                 │
  │  │ CLIP A급 유사도│   │ 편집 모드     │                 │
  │  │ (보조 검증)    │   │ (기존 A컷 수정)│                │
  │  └────────────────┘   └──────────────┘                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘

  참조 DB:
  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────┐
  │ expression_      │  │ pose_            │  │ db/mlb_style/  │
  │ presets.json     │  │ presets.json     │  │ A급 이미지 DB  │
  │ 10개 (시크/러블리)│  │ 75개 (4카테고리) │  │ director JSON  │
  └──────────────────┘  └──────────────────┘  └────────────────┘
```

**핵심 파일 매핑:**

| 단계 | 파일 | 핵심 함수 | 모델 |
|------|------|----------|------|
| 분석 | `analyzer.py` | `analyze_outfit()`, `analyze_pose()`, `analyze_expression()` | VISION_MODEL |
| 조립 | `prompt_builder_v2.py` | `build_prompt()`, `enhance_prompt_for_retry()` | - |
| 스타일 | `style_director.py` | `select_style_with_director()` | - |
| 생성 | `generator_v2.py` | `generate_brandcut()` | IMAGE_MODEL |
| 검수 | `validator_v2.py` | `BrandcutValidator.validate()` — 14기준 | VISION_MODEL |
| 루프 | `retry_generator_v2.py` | `generate_with_validation()` | - |
| 편집 | `edit_generator.py` | `edit_with_validation()` | IMAGE_MODEL |
| 보조 | `clip_validator.py` | `score_a_grade_similarity()` | CLIP |

---

## 2. 배경 교체 (Background Swap)

**스킬:** `.claude/skills/배경교체_background-swap/SKILL.md`
**코어:** `core/background_swap/`
**컨셉:** 인물 97% 스케일 고정 + 구조물 위치 고정, 스타일만 Reskinning

```
  ┌─────────┐     ┌─────────────────┐
  │  소스   │     │  배경 스타일     │
  │ 이미지  │     │ (프리셋 or 직접) │
  └────┬────┘     └───────┬─────────┘
       │                  │
       └────────┬─────────┘
                │
                ▼
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       1. VFX 물리 분석 (VISION_MODEL)         │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ analyzer.py                          │    │     │
  │   │  │                                      │    │     │
  │   │  │ analyze_model_physics()              │    │     │
  │   │  │  → geometry (카메라 앵글, 수평선)    │    │     │
  │   │  │  → lighting (광원 방향, 색온도)      │    │     │
  │   │  │  → pose_dependency (기대기/앉기)     │    │     │
  │   │  │  → physics_anchors (접촉점, 그림자)  │    │     │
  │   │  │  → semantic_style (추천 장소)        │    │     │
  │   │  │                                      │    │     │
  │   │  │ detect_source_type()                 │    │     │
  │   │  │  → outdoor | studio | indoor         │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       2. 프롬프트 조립 (7레이어 누적)         │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ prompt_builder.py                    │    │     │
  │   │  │                                      │    │     │
  │   │  │ build_background_prompt()            │    │     │
  │   │  │                                      │    │     │
  │   │  │ Layer 1: BASE_PRESERVATION (97%)     │    │     │
  │   │  │ Layer 2: STRUCTURE_STYLE_TRANSFORM   │    │     │
  │   │  │ Layer 3: ONE_UNIT (차량 감지 시)     │    │     │
  │   │  │ Layer 4: VFX 가이드라인              │    │     │
  │   │  │ Layer 5: 배경 VLM 분석 결과          │    │     │
  │   │  │ Layer 6: 차량/바닥/색보정 지시문     │    │     │
  │   │  │ Layer 7: 사용자 배경 스타일          │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   │                                              │     │
  │   │  ┌──────────────┐  ┌──────────────────────┐  │     │
  │   │  │ templates.py │  │ presets.py            │  │     │
  │   │  │ 프롬프트 상수│  │ 15종 배경 스타일     │  │     │
  │   │  │ ONE_UNIT     │  │  콘크리트 4 | 도시 7 │  │     │
  │   │  │ PRESERVATION │  │  스튜디오 4          │  │     │
  │   │  └──────────────┘  └──────────────────────┘  │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       3. 이미지 생성 (IMAGE_MODEL)            │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ generator.py                         │    │     │
  │   │  │                                      │    │     │
  │   │  │ generate_background_swap()           │    │     │
  │   │  │  → 원본 비율 자동 계산               │    │     │
  │   │  │  → 10개 지원 비율 중 근접값 선택     │    │     │
  │   │  │                                      │    │     │
  │   │  │ 3가지 모드:                          │    │     │
  │   │  │  Fast    — 검증 없이 빠른 생성       │    │     │
  │   │  │  Quality — 매 생성마다 9-criteria     │    │     │
  │   │  │  Sweep   — 일괄 생성 후 검증         │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │       4. 검수 + 재생성 루프                   │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ validator.py                         │    │     │
  │   │  │                                      │    │     │
  │   │  │ 9-criteria 검수:                     │    │     │
  │   │  │  model_preservation   25%  =100 필수 │    │     │
  │   │  │  relight_naturalness  15%            │    │     │
  │   │  │  lighting_match       12%            │    │     │
  │   │  │  ground_contact       10%            │    │     │
  │   │  │  physics_plausibility 10%  >=50 필수 │    │     │
  │   │  │  edge_quality          8%            │    │     │
  │   │  │  prop_style_cons.      5%            │    │     │
  │   │  │  color_temp_compl.     5%  >=80 필수 │    │     │
  │   │  │  perspective_match    10%  >=70 필수 │    │     │
  │   │  │                                      │    │     │
  │   │  │ Pass: 총점 95+ AND 필수 4개 충족     │    │     │
  │   │  │                                      │    │     │
  │   │  │ 재시도:                              │    │     │
  │   │  │  Temp 0.2 → 0.1 → 0.05 감소         │    │     │
  │   │  │  ENHANCEMENT_RULES 11종 보강         │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │                 ┌─────────────────┐                    │
  │                 │    결과 반환     │                    │
  │                 │ image + score    │                    │
  │                 │ + 9-criteria     │                    │
  │                 └─────────────────┘                    │
  └────────────────────────────────────────────────────────┘
```

**핵심 파일 매핑:**

| 단계 | 파일 | 핵심 함수 | 모델 |
|------|------|----------|------|
| VFX 분석 | `analyzer.py` | `analyze_model_physics()`, `detect_source_type()` | VISION_MODEL |
| 프롬프트 | `prompt_builder.py` | `build_background_prompt()` — 7레이어 | - |
| 상수 | `templates.py` | `BASE_PRESERVATION_PROMPT`, `ONE_UNIT_PROMPTS` | - |
| 프리셋 | `presets.py` | `get_style_preset()` — 15종 | - |
| 생성 | `generator.py` | `swap()`, `generate_background_swap()` | IMAGE_MODEL |
| 검수 | `validator.py` | `BackgroundSwapValidator.validate()` — 9기준 | VISION_MODEL |
| 레지스트리 | `workflow_validator.py` | `@ValidatorRegistry.register` 래퍼 | - |

---

## 3. AI 인플루언서

**스킬:** `.claude/skills/AI인플루언서_ai-influencer/SKILL.md`
**코어:** `core/ai_influencer/`
**컨셉:** Image-First (이미지가 주역, 텍스트는 보조)

```
  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐
  │ 얼굴 │  │ 착장 │  │ 포즈 │  │ 표정 │  │ 배경 │
  │ imgs │  │ imgs │  │ img  │  │ img  │  │ img  │
  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘
     │         │         │         │         │
     └────┬────┘         └────┬────┘         │
          │                   │              │
          ▼                   ▼              ▼
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │     1. VLM 분석 6종 (1회만 실행, 재사용)        │   │
  │  │                                                 │   │
  │  │  ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │   │
  │  │  │hair_analyzer│ │face_analyzer│ │expression_│ │   │
  │  │  │ .py         │ │ .py (v3.0)  │ │ analyzer  │ │   │
  │  │  │             │ │             │ │ .py       │ │   │
  │  │  │ 헤어 스타일 │ │ 얼굴 특징   │ │ 눈/시선/  │ │   │
  │  │  │ 컬러/질감   │ │ 앵커링용    │ │ 입/윙크   │ │   │
  │  │  └─────────────┘ └─────────────┘ └───────────┘ │   │
  │  │                                                 │   │
  │  │  ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │   │
  │  │  │pose_analyzer│ │background_  │ │compatibil-│ │   │
  │  │  │ .py         │ │ analyzer.py │ │ ity.py    │ │   │
  │  │  │             │ │             │ │           │ │   │
  │  │  │ 25+ 신체    │ │ 제공요소    │ │ 포즈-배경 │ │   │
  │  │  │ 필드 추출   │ │ 가능 stance │ │ 호환 체크 │ │   │
  │  │  │ 4자/L자 다리│ │             │ │           │ │   │
  │  │  └─────────────┘ └─────────────┘ └───────────┘ │   │
  │  └─────────────────────┬───────────────────────────┘   │
  │                        │                               │
  │                        ▼                               │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │     2. 프롬프트 조립                             │   │
  │  │                                                 │   │
  │  │  ┌──────────────────────────────────────────┐   │   │
  │  │  │ prompt_builder.py                        │   │   │
  │  │  │                                          │   │   │
  │  │  │ build_schema_prompt()                    │   │   │
  │  │  │                                          │   │   │
  │  │  │ 핵심 로직:                               │   │   │
  │  │  │  - 프레이밍 기반 자동 생략               │   │   │
  │  │  │    CU/MCU → 하체 전부 제거               │   │   │
  │  │  │    MS/MFS → 다리/신발 제거               │   │   │
  │  │  │    FS/WS  → 전체 묘사                    │   │   │
  │  │  │  - 4자 다리 → FIGURE-4 경고 블록         │   │   │
  │  │  │  - 네거티브 자동 추가                    │   │   │
  │  │  └──────────────────────────────────────────┘   │   │
  │  └─────────────────────┬───────────────────────────┘   │
  │                        │                               │
  │                        ▼                               │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │     3. 이미지 생성 (IMAGE_MODEL)                 │   │
  │  │                                                 │   │
  │  │  ┌──────────────────────────────────────────┐   │   │
  │  │  │ pipeline.py — 유일한 진입점!              │   │   │
  │  │  │                                          │   │   │
  │  │  │ generate_full_pipeline()                  │   │   │
  │  │  │  └→ send_image_request()                 │   │   │
  │  │  │                                          │   │   │
  │  │  │ API 전송 순서:                           │   │   │
  │  │  │  1. 프롬프트 텍스트                      │   │   │
  │  │  │  2. [POSE REFERENCE] img                 │   │   │
  │  │  │  3. [EXPRESSION REFERENCE] img           │   │   │
  │  │  │  4. [FACE 1~3] 얼굴 imgs                │   │   │
  │  │  │  5. [OUTFIT 1~N] 착장 imgs              │   │   │
  │  │  │  6. [BACKGROUND REFERENCE] img           │   │   │
  │  │  │  7. [POSE REMINDER] 재강조               │   │   │
  │  │  └──────────────────────────────────────────┘   │   │
  │  │                                                 │   │
  │  │  ┌──────────────┐                               │   │
  │  │  │ generator.py │  ← 저수준 (직접 호출 금지!)   │   │
  │  │  └──────────────┘                               │   │
  │  └─────────────────────┬───────────────────────────┘   │
  │                        │                               │
  │                        ▼                               │
  │  ┌─────────────────────────────────────────────────┐   │
  │  │     4. 검수 + 재생성 루프                        │   │
  │  │                                                 │   │
  │  │  ┌──────────────────────────────────────────┐   │   │
  │  │  │ validator_v2.py                          │   │   │
  │  │  │                                          │   │   │
  │  │  │ 7개 기준 검수:                           │   │   │
  │  │  │  face_consistency    40%  (최우선!)       │   │   │
  │  │  │  realism             25%                 │   │   │
  │  │  │  scenario_fit        15%                 │   │   │
  │  │  │  skin_condition      10%                 │   │   │
  │  │  │  anti_polish_factor  10%                 │   │   │
  │  │  │                                          │   │   │
  │  │  │ 재시도: VLM 분석 재사용 (비용 절약)      │   │   │
  │  │  │  enhancement_rules만 추가               │   │   │
  │  │  │  Temp 0.05씩 감소                       │   │   │
  │  │  └──────────────────────────────────────────┘   │   │
  │  └─────────────────────┬───────────────────────────┘   │
  │                        │                               │
  │                        ▼                               │
  │                ┌─────────────────┐                     │
  │                │    결과 반환     │                     │
  │                │ image + analysis │                     │
  │                │ + validation     │                     │
  │                └─────────────────┘                     │
  └────────────────────────────────────────────────────────┘

  참조 DB:
  ┌─────────────────┐ ┌─────────────────┐ ┌──────────────────┐ ┌──────────────────┐
  │ pose_presets    │ │ expression_     │ │ background_      │ │ db/ai_influencer/│
  │ .json          │ │ presets.json    │ │ presets.json     │ │ {캐릭터}/        │
  │ 75개 포즈      │ │ 10개 표정      │ │ 96개 배경        │ │ profile.json     │
  │ (4 카테고리)    │ │ (시크/러블리)   │ │ (9 카테고리)      │ │ + face/ imgs     │
  └─────────────────┘ └─────────────────┘ └──────────────────┘ └──────────────────┘
```

**핵심 파일 매핑:**

| 단계 | 파일 | 핵심 함수 | 모델 |
|------|------|----------|------|
| 헤어 분석 | `hair_analyzer.py` | `analyze_hair()` | VISION_MODEL |
| 얼굴 분석 | `face_analyzer.py` | `analyze_face()` (v3.0) | VISION_MODEL |
| 표정 분석 | `expression_analyzer.py` | `ExpressionAnalyzer.analyze()` | VISION_MODEL |
| 포즈 분석 | `core/modules/pose_analyzer.py` | `PoseAnalyzer.analyze()` — 25+ 필드 | VISION_MODEL |
| 배경 분석 | `background_analyzer.py` | `BackgroundAnalyzer.analyze()` | VISION_MODEL |
| 호환 체크 | `compatibility.py` | `CompatibilityChecker.check()` | - |
| 프롬프트 | `prompt_builder.py` | `build_schema_prompt()` — 프레이밍 기반 생략 | - |
| 생성 | `pipeline.py` | `generate_full_pipeline()` — 유일한 진입점 | IMAGE_MODEL |
| 검수 | `validator_v2.py` | `AIInfluencerValidatorV2` — 7기준 | VISION_MODEL |
| 프리셋 | `presets.py` | `load_preset()`, `load_mlb_preset()` | - |
| 캐릭터 | `character.py` | `load_character()`, `Character` | - |

---

## 4. 착장 스왑 (Outfit Swap)

**스킬:** `.claude/skills/착장_outfit-swap/SKILL.md`
**코어:** `core/outfit_swap/`
**컨셉:** EDITING MODE — 소스 이미지가 편집 캔버스, 옷만 교체

```
  ┌─────────┐     ┌──────────────┐
  │  소스   │     │  착장 이미지  │
  │ 이미지  │     │  1~10장      │
  └────┬────┘     └──────┬───────┘
       │                 │
       └────────┬────────┘
                │
                ▼
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │   ┌──────────────────────────────────────────────┐     │
  │   │    1. VLM 분석 (VISION_MODEL)                │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ analyzer.py                          │    │     │
  │   │  │                                      │    │     │
  │   │  │ analyze_source_for_swap()            │    │     │
  │   │  │  → face, pose, body_type, background │    │     │
  │   │  │  ※ 기록용만! 프롬프트에 넣지 않음    │    │     │
  │   │  │                                      │    │     │
  │   │  │ analyze_outfit_items()               │    │     │
  │   │  │  → item_type, color, material, logo  │    │     │
  │   │  │  → 착장 이미지별 개별 분석           │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │    2. 프롬프트 조립 (EDITING MODE)            │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ prompt_builder.py                    │    │     │
  │   │  │  └→ templates.py                     │    │     │
  │   │  │                                      │    │     │
  │   │  │ build_outfit_swap_prompt()           │    │     │
  │   │  │                                      │    │     │
  │   │  │ EDITING MODE 핵심 지시:              │    │     │
  │   │  │  LOCKED: face, pose, bg, scale=1.0   │    │     │
  │   │  │  SWAP:   outfit only                 │    │     │
  │   │  │  PIXEL-LOCKED 지시문                 │    │     │
  │   │  │  ANTI-DRIFT 지시문                   │    │     │
  │   │  │                                      │    │     │
  │   │  │ ※ 소스 분석 텍스트를 넣지 않음!      │    │     │
  │   │  │   (정보 손실 방지 = 품질 핵심)        │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │    3. 이미지 생성 (IMAGE_MODEL)               │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ generator.py                         │    │     │
  │   │  │                                      │    │     │
  │   │  │ generate_with_validation()           │    │     │
  │   │  │                                      │    │     │
  │   │  │ API Parts 전송 순서:                 │    │     │
  │   │  │  1. 프롬프트 텍스트                  │    │     │
  │   │  │  2. [SOURCE IMAGE - EDITING CANVAS]  │    │     │
  │   │  │  3. [OUTFIT REFERENCE 1]             │    │     │
  │   │  │  4. [OUTFIT REFERENCE 2] ...         │    │     │
  │   │  │                                      │    │     │
  │   │  │ 비율: "auto" → detect_aspect_ratio() │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │   ┌──────────────────────────────────────────────┐     │
  │   │    4. 검수 + 재생성 루프                      │     │
  │   │                                              │     │
  │   │  ┌──────────────────────────────────────┐    │     │
  │   │  │ validator.py                         │    │     │
  │   │  │                                      │    │     │
  │   │  │ 5개 기준 검수:                       │    │     │
  │   │  │  outfit_accuracy      35%  AF<70     │    │     │
  │   │  │  face_identity        25%  AF<80     │    │     │
  │   │  │  pose_preservation    25%  AF<90     │    │     │
  │   │  │  outfit_draping       10%            │    │     │
  │   │  │  background_preserv.   5%            │    │     │
  │   │  │                                      │    │     │
  │   │  │ Pass: 총점 92+ AND 필수 3개 충족     │    │     │
  │   │  │                                      │    │     │
  │   │  │ 재시도 (다른 워크플로와 반대!):       │    │     │
  │   │  │  Temp 0.2 → 0.25 → 0.30 (증가!)     │    │     │
  │   │  │  ENHANCEMENT_RULES 기반 보강         │    │     │
  │   │  └──────────────────────────────────────┘    │     │
  │   └──────────────────────┬───────────────────────┘     │
  │                          │                             │
  │                          ▼                             │
  │                 ┌─────────────────┐                    │
  │                 │    결과 반환     │                    │
  │                 │ image + score    │                    │
  │                 │ + 5-criteria     │                    │
  │                 └─────────────────┘                    │
  └────────────────────────────────────────────────────────┘
```

**핵심 파일 매핑:**

| 단계 | 파일 | 핵심 함수 | 모델 |
|------|------|----------|------|
| 소스 분석 | `analyzer.py` | `analyze_source_for_swap()` — 기록용만 | VISION_MODEL |
| 착장 분석 | `analyzer.py` | `analyze_outfit_items()` — 개별 분석 | VISION_MODEL |
| 프롬프트 | `prompt_builder.py` → `templates.py` | `build_outfit_swap_prompt()` | - |
| 상수 | `templates.py` | `OUTFIT_SWAP_PROMPT_TEMPLATE`, `PIXEL-LOCKED` | - |
| 생성 | `generator.py` | `generate_with_validation()`, `detect_aspect_ratio()` | IMAGE_MODEL |
| 검수 | `validator.py` | `OutfitSwapValidator.validate()` — 5기준 | VISION_MODEL |

---

## 4개 워크플로 비교 요약

```
┌───────────────┬──────────────┬──────────────┬──────────────┬──────────────┐
│               │  브랜드컷    │  배경 교체    │  인플루언서  │  착장 스왑   │
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ 변경 대상     │  전체 생성   │  배경만      │  전체 생성   │  착장만      │
│ 보존 대상     │  -           │  인물 97%    │  -           │  얼굴+포즈   │
│               │              │              │              │  +배경       │
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ VLM 분석 수   │  3종         │  3종         │  6종         │  2종         │
│ 프롬프트 방식 │  한국어 JSON │  7레이어 누적│  프레이밍    │  EDITING     │
│               │  스키마      │              │  기반 생략   │  MODE        │
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ 검수 기준 수  │  14개        │  9개         │  7개         │  5개         │
│ Pass 기준     │  85+         │  95+         │  70+         │  92+         │
│ 재시도 Temp   │  감소(-0.03) │  감소(절반)  │  감소(-0.05) │  증가(+0.05) │
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ 프리셋 DB     │  MLB A급 DB  │  15종 스타일 │  181+ 프리셋 │  없음        │
│ 고유 기능     │  StyleDirec- │  ONE UNIT    │  프레이밍    │  PIXEL-      │
│               │  tor + CLIP  │  (차량 보존) │  자동 생략   │  LOCKED      │
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ 진입 함수     │  generate_   │  swap()      │  generate_   │  generate_   │
│               │  with_       │              │  full_       │  with_       │
│               │  validation()│              │  pipeline()  │  validation()│
├───────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ 코어 파일 수  │  12+         │  7           │  12          │  5           │
│ 스킬 문서     │  brand-cut/  │  background- │  ai-influen- │  outfit-     │
│               │  SKILL.md    │  swap/       │  cer/        │  swap/       │
│               │              │  SKILL.md    │  SKILL.md    │  SKILL.md    │
└───────────────┴──────────────┴──────────────┴──────────────┴──────────────┘
```

---

## 모듈 의존성 그래프

```
                          ┌────────────────────┐
                          │   core/config.py   │
                          │  IMAGE_MODEL       │
                          │  VISION_MODEL      │
                          └────────┬───────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
           ┌──────────────┐ ┌──────────┐ ┌──────────────┐
           │ core/api.py  │ │core/     │ │core/         │
           │ get_next_    │ │options.py│ │validators/   │
           │ api_key()    │ │ ratios   │ │ base.py      │
           │              │ │ costs    │ │ registry.py  │
           └──────┬───────┘ └────┬─────┘ └──────┬───────┘
                  │              │               │
      ┌───────────┴──────────────┴───────────────┴──────────┐
      │                                                     │
      │            core/outfit_analyzer.py                   │
      │            (착장 VLM 분석 — 3개 워크플로 공용)       │
      │                                                     │
      ├─────────────┬─────────────┬─────────────────────────┤
      │             │             │                         │
      ▼             ▼             ▼                         ▼
┌───────────┐ ┌───────────┐ ┌───────────────┐    ┌──────────────┐
│  brand-   │ │background_│ │ai_influencer/ │    │ outfit_swap/ │
│  cut/     │ │  swap/    │ │               │    │              │
│           │ │           │ │ pipeline.py   │    │ generator.py │
│ 12+ files │ │  7 files  │ │ 12 files      │    │  5 files     │
│           │ │           │ │               │    │              │
│  v1 + v2  │ │ Fast/Qual │ │ Image-First   │    │ EDITING MODE │
│  + edit   │ │ /Sweep    │ │ 프레이밍 생략 │    │ PIXEL-LOCKED │
└───────────┘ └───────────┘ └───────────────┘    └──────────────┘
      │             │             │                      │
      │             │             │                      │
      ▼             ▼             ▼                      ▼
 ┌──────────────────────────────────────────────────────────┐
 │              ValidatorRegistry                           │
 │  BRANDCUT → BrandcutValidator (14기준)                   │
 │  BACKGROUND_SWAP → BackgroundSwapValidator (9기준)       │
 │  AI_INFLUENCER → AIInfluencerValidatorV2 (7기준)        │
 │  OUTFIT_SWAP → OutfitSwapValidator (5기준)              │
 └──────────────────────────────────────────────────────────┘
```

---

## 스킬 문서 위치

| 워크플로 | 스킬 경로 | 치트시트 |
|---------|----------|---------|
| 브랜드컷 | `.claude/skills/브랜드컷_brand-cut/SKILL.md` | `mlb-prompt-cheatsheet.md` |
| 배경교체 | `.claude/skills/배경교체_background-swap/SKILL.md` | `background-swap-cheatsheet.md` |
| 인플루언서 | `.claude/skills/AI인플루언서_ai-influencer/SKILL.md` | (SKILL.md 내 포함) |
| 착장스왑 | `.claude/skills/착장_outfit-swap/SKILL.md` | (없음) |

---

*Generated: 2026-03-05 | FNF AX Team*
