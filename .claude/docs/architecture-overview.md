# FNF Studio 아키텍처 오버뷰

```
                                ┌─────────────────────┐
                                │      사용자 입력      │
                                │                     │
                                │  얼굴 / 착장 /      │
                                │  배경 / 포즈 이미지  │
                                └──────────┬──────────┘
                                           │
                                           ▼
          ┌────────────────────────────────────────────────────────┐
          │                                                        │
          │                    Claude 스킬                         │
          │              (.claude/skills/)                         │
          │                                                        │
          │   사용자와 코어 모듈 사이의 "레시피"                   │
          │                                                        │
          │   ┌──────────────────────────────────────────────┐    │
          │   │  SKILL.md        워크플로 실행 절차 안내     │    │
          │   │  cheatsheet.md   브랜드 규칙 (DO / DON'T)    │    │
          │   │  presets.md      프리셋 옵션 가이드           │    │
          │   └──────────────────────────────────────────────┘    │
          │                                                        │
          │   역할:                                                │
          │   1. 사용자에게 필요한 옵션 질문                      │
          │   2. 어떤 코어 모듈을 어떤 순서로 호출할지 지시       │
          │   3. 브랜드 규칙/금지 조합 가드레일                   │
          │   4. 검수 기준 및 재생성 조건 정의                    │
          │                                                        │
          └────────────────────────┬───────────────────────────────┘
                                   │
                                   │ 스킬이 지시하는 대로
                                   │ 코어 모듈을 순서대로 호출
                                   │
                              ┌────┴────┐
                              │         │
                              ▼         ▼
               ┌──────────────────┐  ┌──────────────────┐
               │   VLM 분석기     │  │   프리셋 DB      │
               │                  │  │                  │
               │  착장 분석       │  │  포즈 프리셋     │
               │  포즈 분석       │  │  표정 프리셋     │
               │  표정 분석       │  │  배경 프리셋     │
               │  배경 분석       │  │  시나리오 프리셋 │
               │  헤어/얼굴 분석  │  │  소재 라이브러리 │
               └────────┬─────────┘  └────────┬─────────┘
                        │                     │
                        └──────────┬──────────┘
                                   │
                        ┌──────────▼──────────┐
                        │                     │
                        │  브랜드 치트시트     │
                        │  (MLB DNA / 금지조합)│
                        │                     │
                        └──────────┬──────────┘
                                   │
                                   ▼
          ┌────────────────────────────────────────────────────────┐
          │                  프롬프트 빌더                         │
          │                                                        │
          │   분석 결과 + 프리셋 + 브랜드 규칙 + 사용자 옵션     │
          │                       ▼                                │
          │              한국어 JSON 스키마 프롬프트               │
          │   { 주제, 모델, 포즈, 착장, 배경, 조명색감 }          │
          └────────────────────────┬───────────────────────────────┘
                                   │
                                   ▼
          ┌────────────────────────────────────────────────────────┐
          │                     생성기                             │
          │                                                        │
          │         프롬프트 + 참조 이미지 → Gemini API           │
          │                       ▼                                │
          │                   생성 이미지                          │
          └────────────────────────┬───────────────────────────────┘
                                   │
                                   ▼
          ┌────────────────────────────────────────────────────────┐
          │                     검증기                             │
          │                                                        │
          │             통과 ──────── 탈락                         │
          │              │              │                          │
          │              ▼              ▼                          │
          │           결과 저장     재시도 (최대 2회)              │
          │                        원인 진단 → 프롬프트 수정      │
          └────────────────────────────────────────────────────────┘


 ═══════════════════════════════════════════════════════════════════

  스킬 → 코어 모듈 연결 구조

 ═══════════════════════════════════════════════════════════════════


  Claude 스킬 (레시피)              코어 모듈 (실행 코드)
  ─────────────────────            ───────────────────────

  .claude/skills/                  core/
  브랜드컷_brand-cut/              brandcut/
  ┌─────────────────┐             ┌─────────────────────┐
  │ SKILL.md        │────호출────►│ analyzer.py         │
  │ "1. 착장 분석"  │             │ prompt_builder.py   │
  │ "2. 프롬프트"   │             │ generator.py        │
  │ "3. 생성"       │             │ validator.py        │
  │ "4. 검수"       │             │ retry_generator.py  │
  │                 │             └─────────────────────┘
  │ mlb-prompt-     │────참조────► 브랜드 규칙 적용
  │  cheatsheet.md  │
  └─────────────────┘

  스킬 = "무엇을, 어떤 순서로, 어떤 규칙으로"
  코어 = "실제로 실행하는 코드"


 ═══════════════════════════════════════════════════════════════════

  워크플로 현황

 ═══════════════════════════════════════════════════════════════════


  ●━━ [운영중] ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━●

     ┌──────────┐   ┌──────────────┐   ┌──────────┐   ┌──────────┐
     │ 브랜드컷 │   │AI 인플루언서 │   │ 배경 교체│   │ 착장 변경│
     │          │   │              │   │          │   │          │
     │ 착장     │   │ 포즈         │   │ 배경     │   │ 착장     │
     │ 포즈     │   │ 표정         │   │ 인물보존 │   │ 얼굴보존 │
     │ 표정     │   │ 헤어/얼굴    │   │ 리라이팅 │   │ 포즈보존 │
     │ 배경     │   │ 배경/착장    │   │          │   │          │
     └──────────┘   └──────────────┘   └──────────┘   └──────────┘


  ○── [개발중] ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─○

     ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
     │ 얼굴교체 │   │   셀카   │   │ 포즈변경 │   │ 포즈복제 │
     │          │   │          │   │          │   │          │
     │ 얼굴     │   │ 시나리오 │   │ 포즈     │   │ 레퍼포즈 │
     │ 보존     │   │ 프리셋   │   │ 보존     │   │ 보존     │
     └──────────┘   └──────────┘   └──────────┘   └──────────┘
     ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
     │다중 얼굴 │   │ 이커머스 │   │  핏 변형 │   │시딩 UGC  │
     │  교체    │   │          │   │          │   │          │
     │ 감지     │   │ 상세페이지│   │ 팬츠     │   │ 역검증   │
     │ 매핑     │   │ Figma    │   │ 핏       │   │ (자연스러움)│
     └──────────┘   └──────────┘   └──────────┘   └──────────┘


  ○∙∙ [개발예정] ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ∙ ○

     ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
     │제품 디자인│   │ 소재 생성│   │ 슈즈 3D │   │ 제품 연출│
     │          │   │          │   │          │   │          │
     │ 착장분석 │◄──│ 소재     │   │ 3D 파이프│   │ 착장분석 │
     │ 배경분석 │   │ 라이브러리│   │ 라인     │   │ 배경/시나리오│
     └──────────┘   └──────────┘   └──────────┘   └──────────┘
     ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
     │  그래픽  │   │마네킹 착장│   │마네킹 포즈│   │ 채널 배너│
     │          │   │          │   │          │   │          │
     │ 그래픽   │   │ 착장분석 │◄──│ 포즈분석 │   │ Figma    │
     │ 엔진     │   │ 포즈분석 │   │ 착장분석 │   │ 레이아웃 │
     └──────────┘   └──────────┘   └──────────┘   └──────────┘


 ═══════════════════════════════════════════════════════════════════

  조합 가능한 코어 블록

 ═══════════════════════════════════════════════════════════════════


  분석기 블록              프리셋 블록             인프라 블록
  ─────────────           ──────────────          ──────────────
  ┌───────────────┐       ┌───────────────┐      ┌───────────────┐
  │ 착장 분석     │       │ 포즈 프리셋   │      │ 설정          │
  │ (색상/로고/   │       │ (관절/앵글)   │      │ (모델명)      │
  │  실루엣)      │       ├───────────────┤      ├───────────────┤
  ├───────────────┤       │ 표정 프리셋   │      │ API           │
  │ 배경 분석     │       │ (시선/표정)   │      │ (키 로테이션) │
  │ (장소/조명)   │       ├───────────────┤      ├───────────────┤
  ├───────────────┤       │ 배경 프리셋   │      │ 옵션          │
  │ 포즈 분석     │       │ (배경 스타일) │      │ (비율/해상도) │
  │ (관절/프레임) │       ├───────────────┤      ├───────────────┤
  ├───────────────┤       │ 시나리오      │      │ 검증기        │
  │ 표정 분석     │       │ (UGC 상황)    │      │ 레지스트리    │
  │ (표정/시선)   │       ├───────────────┤      │ (자동 등록)   │
  ├───────────────┤       │ 핏 프리셋     │      └───────────────┘
  │ 헤어 분석     │       │ (핏 변형)     │
  │ (스타일/컬러) │       ├───────────────┤
  ├───────────────┤       │ 소재 라이브러리│
  │ 얼굴 분석     │       │ (원단 텍스처) │
  │ (얼굴 특징)  │       └───────────────┘
  └───────────────┘


  조합 예시:

  ┌────────┐   ┌────────┐   ┌────────┐
  │착장분석│ + │포즈분석│ + │포즈    │ ──► 마네킹 착장 [개발예정]
  │        │   │        │   │프리셋  │     (신규: 프롬프트빌더만)
  └────────┘   └────────┘   └────────┘

  ┌────────┐   ┌────────┐   ┌────────┐
  │착장분석│ + │배경분석│ + │시나리오│ ──► 제품 연출 [개발예정]
  │        │   │        │   │프리셋  │     (신규: 프롬프트빌더만)
  └────────┘   └────────┘   └────────┘

  ┌────────┐   ┌────────┐
  │포즈분석│ + │착장분석│ ──────────────► 마네킹 포즈 [개발예정]
  │        │   │        │                 (신규: 프롬프트빌더만)
  └────────┘   └────────┘


 ═══════════════════════════════════════════════════════════════════

  범례

  ●━━━ [운영중]     실무 투입 완료              4개
  ○─── [개발중]     코드 완료, 테스트 단계      8개
  ○∙∙∙ [개발예정]   코어 조합으로 빠르게 가능   8개+

  Claude 스킬 = 워크플로의 "레시피" (실행 절차 + 브랜드 규칙)
  코어 모듈   = 실제 실행하는 "재료 + 도구"

 ═══════════════════════════════════════════════════════════════════
```
